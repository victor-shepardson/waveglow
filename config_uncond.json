{
    "train_config": {
        "output_directory": "checkpoints",
        "epochs": 100000,
        "learning_rate": 1e-4,
        "sigma": 1.0,
        "iters_per_checkpoint": 1000,
        "batch_size": 4,
        "seed": 1234,
        "checkpoint_path": "",
        "is_fp16": true
    },
    "data_config": {
        "sanity_check": null,
        "training_files":"common_voice_esperanto_validated.txt",
        "segment_length": 16000,
        "sampling_rate": 16000
    },

    "waveglow_config": {
        "n_flows": 9,
        "n_group": 8,
        "n_early_every": 3,
        "n_early_size": 2,
        "WN_config": {
            "n_layers": 10,
            "n_channels": 256,
            "kernel_size": 3
        }
    }
}
